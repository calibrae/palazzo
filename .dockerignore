# Build context trimmed to what `cargo build` actually needs:
# Cargo.toml, Cargo.lock, src/. Everything else is noise (or huge).
target
!target/
!target/release/
!target/release/palazzo
.git
.github
.fastembed_cache
.palazzo
.memqdrant
.claude
docs
inbox
deploy
scripts
*.md
resume.sh
.env
.dockerignore
Dockerfile
docker-compose.yml
